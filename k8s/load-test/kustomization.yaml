apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: microservice-app

resources:
  - job.yaml
  - networkpolicy.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: k6-load-test-script
    files:
      - catalogue-orders-load.js
